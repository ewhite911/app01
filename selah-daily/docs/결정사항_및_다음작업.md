# 결정사항 및 다음 작업

작성일 2026-09-17, 갱신 2026-09-17. 합의사항의 코드 반영 상태와 다음 작업 순서입니다. 다른 PC에서 이어갈 때 이 문서부터 봅니다.

## 1. 현재 코드 상태 (반영 완료)

- 화면 6개: Today(말씀→기도제목 체크+3분 타이머→Amen→YouTube), Prayers, Paywall, ThankYou, HowItsRun, Settings
- SQLite 로컬 저장, 계정·서버·AI·광고·앱 내 오디오·스트릭 없음
- 365구절: BSB(퍼블릭 도메인) 원문 대조 검증본 `src/data/verses.json`
- 결제: 월 $3.99 단일, 7일 체험, RevenueCat(키 없으면 모의 모드)
- 체험 종료 2일 전 로컬 알림 + 캘린더 일정 추가(선택)
- 무료 기도제목 한도 `config.freePrayerLimit = 30`
- 검증: `tsc` 통과, Android 번들 export 성공. **실기기 테스트는 아직 안 함**

## 2. 합의사항 8건 반영 완료 (2026-09-17)

2026-09-17 대화에서 합의한 8건을 모두 코드에 넣었습니다.

| # | 항목 | 반영 위치 |
|---|---|---|
| 1 | 무료 기도제목 10 → 30 | `src/theme.ts` `config.freePrayerLimit` |
| 2 | 후원형 결제 문구 | `src/copy.ts` `paywallA` → `PaywallScreen` |
| 3 | Subscriber → Member, 설정은 "Membership" | `PaywallScreen`, `SettingsScreen`, `PrayersScreen` |
| 4 | 결제 직후 감사 화면 | `src/screens/ThankYouScreen.tsx`, `member_since` 저장(`db.markMemberSince`) |
| 5 | 운영 공개 페이지 + 정직 서약 4줄 | `src/screens/HowItsRunScreen.tsx`, 문구는 `copy.howItsRun` |
| 6 | 메이커 노트 | `SettingsScreen` 하단, 문구는 `copy.makerNote`(멤버 전용 문단 1개 포함) |
| 7 | 성장하는 나무 5단계 | `src/components/Tree.tsx`, Today·Amen 화면에 배치 |
| 8 | 결제 문구 A/B 스위치 | `config.paywallVariant`('A' 후원형 기본 / 'B' 기능형) |

문구는 전부 `src/copy.ts` 한 곳에 모았습니다. 화면을 뒤지지 않고 문구만 바꿀 수 있습니다.

나무 세부: 겨자씨(0회) → 새싹(3회) → 묘목(7회) → 어린나무(14회) → 물가의 나무(30회). 완료 횟수(`routine_log`)로만 단계가 올라가고 **시들거나 내려가지 않습니다.** 무료 사용자도 5단계 전부 봅니다. 멤버에게는 "Member since" 팻말과 어린나무 단계 이상에서 열매 2개가 추가로 표시될 뿐, 콘텐츠 차이는 없습니다. 이미지 파일 없이 View로 그려서 에셋 의존이 없습니다.

## 2-1. 아직 남은 것

- `src/theme.ts`의 더미 값: `youtubePlaylistId`(현재 `PLxxxx…`), `supportEmail`, `privacyUrl`, `termsUrl` — 스토어 검수 전 필수
- 실기기(또는 에뮬레이터) 설치 테스트
- 결제 문구 A/B의 실제 측정(스토어 분석 이벤트)은 출시 후

## 3. 쓰지 않기로 한 것

- 커피값 비교에 "믿음을 강건하게" 결합(신앙을 파는 것으로 읽힘). 비교하려면 "도구·운영"에 값을 매긴다
- 십자가·어린양·떡과 잔을 모으거나 키우는 요소, 성령의 열매를 보상으로 주는 것
- 실제 식목 제휴: 멤버 수백 명 이후 연 1회 행사로만 재검토(비용·검증·정치색)
- AI 인물이 1인칭 후기를 말하는 영상

## 4. 숏폼·채널

- 형식: 얼굴 없음(뒷모습·손·폰 화면). 5편 구성과 프롬프트는 `이미지_가이드_및_프롬프트.md` 마지막 섹션
- 플랫폼: TikTok·YouTube Shorts 주력, Instagram Reels는 재업로드만. 주 2~3편, AI 생성물 표시
- 폰 화면은 실기기 녹화로 합성(AI로 UI를 그리지 않음)

## 5. 사업·비용

- 사업자: 기존 개인사업자, 업종 "응용 소프트웨어 개발 및 공급업" 확인됨. 과세유형(일반/간이)은 세무사 확인
- Google Play: **조직 계정**, 앱 완성·검증 후 개설($25)
- D-U-N-S: **Apple 경로로 무료 신청 완료**(2026-09-17 기준). 신청 경로 https://developer.apple.com/enroll/duns-lookup/ . D&B 한국의 유료 인증서(신규 50만 원)는 불필요. 검색에 나온 (주)모디텍·모디테크(주)는 다른 회사이므로 선택 금지
- 필수 현금 지출은 출시까지 Play $25뿐. iOS는 $99/년
- 목회자 검수: 현재 콘텐츠(원문 구절만)에는 불필요. 직접 쓴 글(해설·기도문·시즌 팩·번역)이 들어갈 때 1명

## 6. 다음 작업 순서

1. ~~2장 항목 1~8 코드 반영~~ — 2026-09-17 완료
2. `src/theme.ts`의 YouTube 재생목록 ID·지원 이메일·약관·개인정보 URL 입력
3. `eas login` → `eas init` → `eas build -p android --profile development` → 설치·테스트(모의 결제). 실기기 없으면 Android Studio 에뮬레이터로 가능. 실제 Play 결제 검증만 실기기 필요
4. ~~D-U-N-S 신청~~ — 신청 완료
5. 검증 4주(랜딩·인터뷰·숏폼) → 통과 시 Play 조직 계정 → 구독 상품·RevenueCat 키 → 비공개 테스트 12명×14일


## 7. 체크포인트

작업 로그는 Obsidian 볼트 `checkpoints\종교앱-시장조사-20260917`, `checkpoints\기도루틴앱-MVP개발-20260917`에 있습니다(이 저장소에는 포함하지 않음).
